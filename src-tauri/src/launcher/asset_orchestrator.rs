use std::path::Path;

use crate::minecraft::{client_json::fetch_or_get_client_json, manifest::Version};

pub async fn install_version(version: &Version) {
    // todo(chore): .minecraft dir, add dir to all paths
}
